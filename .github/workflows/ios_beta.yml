name: Test CI

on:
  push:
    branches: [ main ]

jobs:
  hello:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "🎉 GitHub Actions 작동 확인 완료!"
